const changelog408 = {
  "changelog.4_0_8.fixed.covers_build":
    "Collection covers and stills load in installed builds without a proxy",
  "changelog.4_0_8.fixed.open_local":
    "Collection details open local files through the system opener",
} as const;

export default changelog408;
